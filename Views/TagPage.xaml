<Page x:Class="StoraDesktop.Views.TagPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="using:StoraDesktop.Models"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="🏷 标签管理" FontSize="18" FontWeight="Bold" Padding="12,8" />
        <Grid Grid.Row="1" Padding="12,0,12,8" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="新标签名称" Text="{x:Bind VM.NewTagName, Mode=TwoWay}" />
            <Button Grid.Column="1" Content="添加" Command="{x:Bind VM.CreateTagCommand}" />
        </Grid>
        <ListView x:Name="TagList" Grid.Row="2" ItemsSource="{x:Bind VM.Tags, Mode=OneWay}" RightTapped="OnRightTap">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:TagItem">
                    <Grid Padding="8,6" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🏷" FontSize="16" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Name}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="2" Text="{x:Bind FileCount}" Opacity="0.6" FontSize="12" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Row="3" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <TextBlock Text="{x:Bind VM.Status, Mode=OneWay}" Padding="8,4" FontSize="12" Opacity="0.6" />
        </Grid>
    </Grid>
</Page>
